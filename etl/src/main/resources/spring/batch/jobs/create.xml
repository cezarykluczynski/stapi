<beans xmlns="http://www.springframework.org/schema/beans"
		xmlns:batch="http://www.springframework.org/schema/batch"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xsi:schemaLocation="
				http://www.springframework.org/schema/batch
				http://www.springframework.org/schema/batch/spring-batch-2.2.xsd
				http://www.springframework.org/schema/beans
				http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
	">
	<batch:job id="CREATE">
		<batch:split id="CREATE_01" task-executor="taskExecutor">
			<batch:flow>
				<batch:step id="STEP_001_CREATE_SERIES">
					<batch:tasklet start-limit="1" allow-start-if-complete="false">
						<batch:chunk
								commit-interval="50"
								reader="seriesReader"
								processor="seriesProcessor"
								writer="seriesWriter"/>
					</batch:tasklet>
					<batch:listeners>
						<batch:listener ref="commonStepExecutionListener"/>
					</batch:listeners>
				</batch:step>
			</batch:flow>
			<batch:flow>
				<batch:step id="STEP_002_CREATE_PRODUCERS">
					<batch:tasklet start-limit="1" allow-start-if-complete="false">
						<batch:chunk
								commit-interval="50"
								reader="performerReader"
								processor="performerProcessor"
								writer="performerWriter" />
					</batch:tasklet>
					<batch:listeners>
						<batch:listener ref="commonStepExecutionListener"/>
					</batch:listeners>
				</batch:step>
			</batch:flow>
		</batch:split>
	</batch:job>
</beans>
