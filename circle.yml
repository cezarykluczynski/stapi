machine:
  java:
    version:
      oraclejdk8
test:
  override:
    - ./gradlew test -i
  post:
    - bash <(curl -s https://codecov.io/bash) -f "build/reports/jacoco/codeCoverageReport/codeCoverageReport.xml"