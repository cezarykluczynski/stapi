get:
  description: Retrival of a single technology (V2)
  parameters:
    - name: uid
      in: query
      type: string
      required: true
      description: Technology unique ID
  responses:
    200:
      schema:
        $ref: '#/definitions/TechnologyV2FullResponse'
    default:
      description: Unexpected error
      schema:
        $ref: '#/definitions/Error'
  tags:
  - Technology
