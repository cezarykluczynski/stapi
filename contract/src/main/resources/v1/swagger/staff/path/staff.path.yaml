get:
  parameters:
    - name: guid
      in: query
      type: string
    - name: apiKey
      in: query
      type: string
  responses:
    200:
      schema:
        $ref: '#/definitions/StaffFullResponse'
    default:
      description: Unexpected error
      schema:
        $ref: '#/definitions/Error'
  tags:
  - Staff
