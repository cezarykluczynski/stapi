get:
  description: Retrival of a single staff (V2)
  parameters:
    - name: uid
      in: query
      type: string
      required: true
      description: Staff unique ID
  responses:
    200:
      schema:
        $ref: '#/definitions/StaffV2FullResponse'
    default:
      description: Unexpected error
      schema:
        $ref: '#/definitions/Error'
  tags:
    - Staff
