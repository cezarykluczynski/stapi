get:
  parameters:
    - name: guid
      in: formData
      type: string
  responses:
    200:
      schema:
        $ref: '#/definitions/CharacterFullResponse'
    default:
      description: Unexpected error
      schema:
        $ref: '#/definitions/Error'
  tags:
  - Character
